更新版本：
V1.0
   -发布release版本
   -支持基本hif交互
   -支持厂商uart/iic 通信接口注册
   -支持固件下载流程
V1.1
   -修改厂商通信接口注册方式
   -修改厂商读image接口注册方式
   -增加自动获取image大小功能
V1.2
   -增加自动识别固件配置信息
   -增加配置雷达工作参数api
V1.3
   -增加动态配置帧周期api
   -增加动态配置触发距离api
   -增加异常处理流程演示
V1.4
   -优化异常处理代码
V1.5
   -增加抗干扰等级映射逻辑
V1.6
   -增加频谱解析流程
     1、具体解析逻辑api（sensor_range_spec_parse）
     2、具体配置下发api（sensor_range_spec_threshold_cfg）
     使用方式需要将dout_mode 处于数据模式（DOUT_DATA_MODE）
     暂时不支持IIC模式下频谱解析
V1.7
   -修改同步流程（0x7e->0x79->0x55->0x79），兼容旧版本
V1.8
   -支持频谱读取命令，及其演示
   -修改动态配置逻辑为配置参数->统一下发(sensor_pram_cfg_startup)
   -规范并支持tlv用法(0x11、0xA0、0xA1)，增加Dout上报状态获取子命令(cmd 0x42、sub_cmd 0x1 )，详情见文档
V1.9
   -支251tlv配置
V2.0
   -支持mode4tlv配置
V2.1
   -优化唤醒逻辑（0x7e->0x79->0x55->0x79），兼容旧版本
V2.2
   -修复261错配不支持的动态参数问题
   -支持固件下载方案（flash/sram）独立firmware_download.c，便于开发者移植
   -支持mrs251、rs2111芯片型号独立host_driver.c，便于开发者移植
V2.3
   -支持主动环境自适应学习api
   -支持固件读版本号api
   -支持移动检测、存在检测独立host_driver.c，便于开发者移植
   -支持周期性保活机制（timer）
   -增加iic主机读操作前，等待从机数据准备时间